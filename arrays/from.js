console.log(Array.from('foo'));
console.log(Array.from([1, 2, 3], x => x + x))
// creates a new, shallow-copied Array instance from an array-like or iterable object.
// first argument => which is required to convert to array
